#include <stdio.h>
#include <conio.h>

main()
{
	int  i = 2;
	float f = 13.5, result;

	//TODO: print out the f/i quotient

    printf("The quotient is: %f", f / i);

	//QUESTION: do you need explicit conversion?

    // we do not need it because the result is in float due to the
	return 0;
}
